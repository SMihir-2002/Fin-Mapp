
import 'package:flutter/material.dart';

class DialogueWidget extends StatelessWidget {
  const DialogueWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const AlertDialog(
      title: Text("Entre all Fields"),
    );
  }
}
